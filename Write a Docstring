def readable_timedelta(days):
    # insert your docstring here
    """Calculating the weeks and days"""

    weeks = days // 7
    remainder = days % 7
    return "{} week(s) and {} day(s)".format(weeks, remainder)
